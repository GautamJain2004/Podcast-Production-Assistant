# Core / LLM
google-generativeai>=0.3.0
google-adk>=0.1.0

# LangGraph
langgraph>=0.2.0
langgraph-checkpoint>=0.1.0

# Pydantic & settings
pydantic>=2.0.0
pydantic-settings>=1.0.0

# Env + HTTP + scraping
python-dotenv>=1.0.0
requests>=2.25.0
types-requests>=2.25.0
beautifulsoup4>=4.9.0
urllib3>=1.26.0
httpx>=0.24.0
aiohttp>=3.8.0
websockets>=11.0.0

# Search / MCP / Tools
google-search-results>=2.4.0
mcp>=1.0.0
mcp-cli                 # keep mcp-cli if you use CLI tools (adjust version as needed)

# Web / form parsing
markdown>=3.0.0
python-multipart>=0.0.6

# Messaging / mqtt
asyncio-mqtt>=0.11.0

# Testing & CI
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Dev / formatting / type checking
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Utilities
rich>=13.0.0

# Optional / helpful for packaging or debugging
tqdm>=4.0.0

# YouTube transcript extraction
youtube-transcript-api>=0.6.0

# Google API for YouTube search (optional)
google-api-python-client>=2.0.0

# Web Frontend
streamlit>=1.28.0
